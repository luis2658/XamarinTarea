<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Question1.Views.TareaDetailPage">
    <ContentPage.ToolbarItems>
        <!--<ToolbarItem Text="Editar" Command="{Binding EditTareaCommand}" />-->
        <ToolbarItem Text="Eliminar" Command="{Binding DeleteTareaCommand}" />
    </ContentPage.ToolbarItems>
    <StackLayout Spacing="20" Padding="15">
        <Label Text="Tarea:" FontSize="Medium" />
        <Label Text="{Binding Task}" FontSize="Small"/>
        <Label Text="Descripción:" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>
        <Label Text="Completado:" FontSize="Medium" />
        <Label Text="{Binding IsCompleted}" FontSize="Small"/>
    </StackLayout>
</ContentPage>